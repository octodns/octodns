---
type: patch
---
Drop the redundant `caa-value-best-practice` validator from the `best-practice` set — `caa-best-practices` already applies the same issue-without-issuewild check at zone scope. It stays registered so it can still be enabled or disabled by name. Closes #1448
