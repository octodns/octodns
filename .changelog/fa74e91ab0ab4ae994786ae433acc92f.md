---
type: minor
---
Add mergers subsystem for combining same-name/type record values from separate sources (e.g. CAA `issue` values) instead of raising `DuplicateRecordException`; merged records are built through `Record.new` and validated like any other record
